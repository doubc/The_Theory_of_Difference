# 待办事项

> 按「理论核心性 × 实现可行性」排序。只保留未完成项。

---

## P0: 本迭代可实现

### 1. 交易信号层（多视角交集）
**对应机制**：守恒通量 + 错觉检测 + 运动态
- 新增 `src/signals.py`：`Signal` dataclass + `generate_signal()`
- 5维突破评分（穿透深度 + 量能扩张 + 通量一致性 + 压缩蓄势 + 驻留比）
- 假突破 5 种模式判定
- `conservation_flux` 显式参与所有信号过滤
- `stability_verdict` 红灯覆盖方向性判断
- 设计文档：`docs/头脑风暴-多视角交集分析.md`

### 2. 5分钟结构的日内节奏分析集成
**对应机制**：最小变易 + 时间压缩
- 代码已有 `src/intraday_rhythm.py`（308行），但未集成到工作台
- 需要接入 Streamlit 页面展示

---

## P1: 待延伸

### 3. FAISS 向量检索替代暴力搜索
**对应机制**：层级 + 自指
- 将不变量向量化后用 FAISS 做 ANN 检索
- 只对 A+B 层建索引，C/D 层不入索引
- 依赖：不变量向量维度固定（当前 10 维）

### 4. 叙事递归追踪
**对应机制**：循环 + 锁定
- 结构的 narrative_context 在不同 cycle 之间如何演化
- 检测"叙事锁定"：市场叙事不再更新，结构进入稳态
- 叙事漂移率可作为质量维度

### 5. 反身性闭环检测
**对应机制**：自指
- 同一结构模板的历史有效性是否在衰减
- 衰减中的模板自动从 A 层降级
- 依赖：`sample/outcome.py` 的后验统计

### 6. 结构库质量报告
**对应机制**：层级 + 自指
- 定期生成全库质量分布报告
- 监控 A 层占比是否在下降（库退化）
- 实现量：~100 行

---

## P2: 探索性（理论前沿）

### 7. 跨时间维度守恒验证
**对应机制**：守恒
- 5分钟结构"消失"时，日线是否"出现"了对应结构
- 差异在不同时间尺度之间的转移通道检测
- 依赖：长时间序列的 5min + 1d 同步数据

### 8. 结构自动生成规则
**对应机制**：自指 + 先天完备
- 从历史样本中自动学习结构模式，而非人工定义规则
- 用 DBSCAN/HDBSCAN 从不变量空间中发现新的结构类型

### 9. 多品种差异转移图谱
**对应机制**：守恒 + 循环
- 构建品种之间的差异转移网络
- 节点 = 品种，边 = 差异转移强度
- 依赖：大量历史数据 + 品种间 Granger 因果检验

---

## 技术债

| 项目 | 优先级 | 说明 |
|------|--------|------|
| `__pycache__` 清理 | 低 | 仓库中不应有 .pyc 文件 |
| 测试覆盖提升 | 中 | 当前 73 个，目标 100+ |
| Parquet 数据验证 | 中 | 新增 Parquet 读写需要测试 |
| C 扩展 Windows 兼容 | 低 | 当前只支持 Linux/macOS |
| 质量分层测试 | 中 | 各维度评分边界情况 |
