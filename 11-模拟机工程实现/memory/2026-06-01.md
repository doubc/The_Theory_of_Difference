# 2026-06-01

## exp_93 诊断与修复

### 问题
- GBC 完全未激活（n_checks=0 所有种子）
- CIV 均值 48.5，是 exp_90 基线 5.25 的 9.2 倍
- 根因：AxiomConstraints.direction 初始化为全零，导致所有 GBC 机制产生零向量

### 修复
- cl/axioms_v2.py: 添加 initial_state 参数，从状态派生方向
- engine/hierarchy_manager.py: 两处创建点传入 initial_state
- 验证通过：local_biases 现在能正确填充 2 个机制

### 提交
- 46029d fix(phase4): activate GBC by initializing A6 direction from state

### GBC 根本原因分析
- **根因**: evolver 的 `run()` 返回值中缺少 `gbc_checks` 键
- GBC `evaluate()` 确实被调用了（`phase2_summary.global_bias_constraint_pass_rate` 有值），但结果没被收集到顶层
- exp_93 脚本读取 `result.get('gbc_checks', [])` → 永远为空 → n_checks=0
- **修复**: 在 evolver return dict 中添加 `gbc_checks` 列表，从 `global_bias_constraint.get_history()` 提取
- 提交: `8e7d33a fix(phase4): expose gbc_checks in evolver return dict`

### CIV 异常分析（mean=48.5 vs exp_90 baseline=5.25）
- 8-seed 分布极度离散: [23, 6, 4, 8, 13, 131, 5, 198]
- seed 742 (198 CIV) 和 seed 542 (131 CIV) 拉高了均值
- 根因可能: GBC 未激活 → 缺少全局偏置约束 → CIV 涌现阈值被突破
- 修复 GBC 后需重新运行 exp_93 验证 CIV 是否回归

### 待办
- 重新运行 exp_93 验证 GBC 修复效果（GBC n_checks 应 > 0）
- 关注 CIV 是否回归正常范围
- 调整涌现阈值（emergence_stability_threshold 可能过低）
