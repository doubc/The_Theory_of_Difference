# 心跳任务摘要 — 2026-05-30 10:14

## 目标

按 HEARTBEAT.md 强制行动要求，执行至少一项实质工作并留痕。

## 执行内容

### 1. Git 维护：提交 09:44 心跳的待提交改动

```
7bf6a30 heartbeat(2026-05-30): 理论浸润 + HEARTBEAT.md 更新 + 项目状态盘点
 2 files changed, 85 insertions(+)
```

### 2. 工程推进：实现 GlobalBiasConstraint 类（P0）

根据 `docs/phase3_global_bias_constraint_design.md` 设计文档，完整实现了全局偏置约束组件：

**文件**: `engine/global_bias_constraint.py`（250 行）

**核心功能**:
- `GlobalBiasConstraint.evaluate()` — 收集各机制局部偏置，计算全局偏置 B_G（加权几何平均），检测方向一致性和强度平衡约束
- `_geometric_mean()` — 在单位球面上进行加权几何平均（Karcher 均值近似）
- `_arithmetic_mean()` — 加权算术平均（可选）
- 历史追踪：`get_history()`, `get_coherence_trend()`, `get_balance_trend()`, `get_pass_rate()`, `reset()`

**约束机制**:
- 方向一致性约束：Coherence = (1/6) Σ cos(∠(B_M^(k), B_G)) ≥ τ_coh（默认 0.6）
- 强度平衡约束：Balance = 1 - (max‖B‖ - min‖B‖)/(max‖B‖) ≥ τ_bal（默认 0.5）

### 3. 编写单元测试（24 个测试）

**文件**: `tests/test_global_bias_constraint.py`

| 测试类 | 测试数 | 覆盖内容 |
|--------|--------|----------|
| TestGlobalBiasConstraintBasics | 6 | 几何平均、算术平均、余弦相似度 |
| TestGlobalBiasConstraintEvaluation | 5 | 方向一致性、强度平衡、耦合强度加权、违反机制识别 |
| TestGlobalBiasConstraintEdgeCases | 3 | 偏置不足、零向量过滤、自定义阈值 |
| TestGlobalBiasConstraintHistory | 8 | 历史累积、趋势、通过率、重置、repr |
| TestGlobalBiasConstraintResult | 2 | 结果数据结构完整性 |

### 4. 全量回归测试

```
816 passed, 23 skipped in 33.26s ✅
```
（原 792 + 新增 24 = 816，无回归）

## 项目状态更新

| 阶段 | 组件 | 状态 |
|------|------|------|
| Phase 3: 全局偏置约束 B_G | 设计文档 | ✅ 已完成 |
| Phase 3: GlobalBiasConstraint 实现 | 代码 | ✅ **本次完成** |
| Phase 3: GlobalBiasConstraint 单元测试 | 测试 | ✅ **本次完成** (24/24) |
| Phase 3: 在 HierarchicalEvolver 中集成 | 集成 | ⏳ 待实现 (P0) |
| Phase 3: _degrade_violating_mechanisms 降级机制 | 代码 | ⏳ 待实现 (P1) |
| Phase 3: 端到端集成测试 | 测试 | ⏳ 待实现 (P1) |
| Phase 3: exp_74 重写（MSI-ODI-叙事） | 实验 | ⏳ 需重写回调机制 |
| Phase 3: 实验五（全局偏置一致性演化） | 实验 | ⏳ 待设计 (P2) |

## Git 状态

```
d5b5943 feat(phase3): 实现 GlobalBiasConstraint 类 + 24 个单元测试 (P0)
7bf6a30 heartbeat(2026-05-30): 理论浸润 + HEARTBEAT.md 更新 + 项目状态盘点
a739845 feat(phase3): 全局偏置约束 B_G 设计文档 + exp_74 实验修复 + 当日心跳记录
```

3 个本地提交待推送。

## 下一步

1. **P0**: 在 `hierarchical_evolver.py` 中添加 `global_bias_constraint` 参数并集成到 P1 评估段
2. **P0**: 实现 `_degrade_violating_mechanisms` 降级机制
3. **P1**: 编写端到端集成测试 `test_phase3_global_bias.py`
4. **P1**: 重写 exp_74 实验框架（适配当前 HierarchicalEvolver API）
5. **P2**: 设计并实现实验五（全局偏置一致性演化）

---
*10:14 心跳记录 — 2026-05-30*
