# 待办事项

> 按「理论核心性 × 实现可行性」排序。只保留未完成项。

---

## P0: 本迭代可实现

### 1. FAISS 向量检索替代暴力搜索
**对应机制**：层级 + 自指
- 将不变量向量化后用 FAISS 做 ANN 检索
- 只对 A+B 层建索引，C/D 层不入索引
- 依赖：不变量向量维度固定（当前 10 维）

### 2. 5分钟结构的日内节奏分析工作台集成
**对应机制**：最小变易 + 时间压缩
- 代码已有 `src/intraday_rhythm.py`（308行），但未集成到工作台 Tab
- 需要接入 Streamlit 页面展示

---

## P1: 待延伸

### 3. 结构自动生成规则
**对应机制**：自指 + 先天完备
- 从历史样本中自动学习结构模式，而非人工定义规则
- 用 DBSCAN/HDBSCAN 从不变量空间中发现新的结构类型

### 4. 结构库质量报告
**对应机制**：层级 + 自指
- 定期生成全库质量分布报告
- 监控 A 层占比是否在下降（库退化）

---

## P2: 探索性（理论前沿）

### 5. 跨时间维度守恒验证（深度）
**对应机制**：守恒
- 5分钟结构"消失"时，日线是否"出现"了对应结构
- 差异在不同时间尺度之间的转移通道检测
- 依赖：长时间序列的 5min + 1d 同步数据

---

## 技术债

| 项目 | 优先级 | 说明 |
|------|--------|------|
| `__pycache__` 清理 | 低 | 仓库中不应有 .pyc 文件 |
| 测试覆盖提升 | 中 | 当前 73 个，目标 100+ |
| C 扩展 Windows 兼容 | 低 | 当前只支持 Linux/macOS |

---

## 已完成（归档）

<details>
<summary>点击展开已完成项</summary>

- ✅ 交易信号层（`signals.py`：5维突破评分 + 假突破模式 + 风险管理）
- ✅ 叙事递归追踪（`narrative_tracker.py`：漂移率 + 锁定检测）
- ✅ 反身性闭环检测（`reflexivity.py`：模板衰减 + 闭环检测）
- ✅ 多品种差异转移图谱（`transfer_network.py`：差异转移 + 热力矩阵）
- ✅ 知识图谱系统（`src/graph/`：GraphStore + StructureGraph + 7品种配置）
- ✅ L1/L2/L3 知识层（`src/knowledge/`：KnowledgeEngine + 27条规则）
- ✅ 知识增强质量评估（`src/quality.py`：6维度评分）
- ✅ C 扩展加速（`_pivots.c` 24x / `_dtw.c` 132x / `_compiler.c` 132x）
- ✅ 多时间维度对比（`multitimeframe/comparator.py`）
- ✅ 数据本地化（`local_store.py`：Parquet 列式压缩）
- ✅ 全市场扫描优化（成交量筛选 + 缓存 + 排序）

</details>
