### **知识图谱不要塞进结构编译器本体，而应嵌在“编译之后、检索之前”：先把 `compile_full()` 的结构结果写入 `GraphStore`，再在 `RetrievalEngine` 里用图谱上下文重排相似案例。**

你这个项目里其实已经有知识图谱底座：`src/graph/__init__.py` 里有 `StructureGraph`，`src/graph/store.py` 里有 `GraphStore`，设计的是结构、Zone、叙事、规则、品种之间的关系图；但从当前 `scripts/daily_pipeline.py` 看，日更流程只做到编译、规则扫描、质量分层、生命周期、相似检索，还没有把图谱写入主流水线。正确嵌入点就是 `compile_full()` 之后、`RetrievalEngine.retrieve()` 之前。项目设计书本身也把“图关系”推荐为 NetworkX，把系统目标定义为结构对象化、相似性检索和条件分布研究，所以知识图谱应服务于“结构关系检索增强”，而不是替代原有相似度。 [GitHub](https://raw.githubusercontent.com/doubc/The_Theory_of_Difference/main/10-%E6%9C%9F%E8%B4%A7%E4%BB%B7%E6%A0%BC%E7%BB%93%E6%9E%84%E6%A3%80%E7%B4%A2%E7%B3%BB%E7%BB%9F/%E9%A1%B9%E7%9B%AE%E8%AE%BE%E8%AE%A1%E4%B9%A6.md) [GitHub](https://raw.githubusercontent.com/doubc/The_Theory_of_Difference/main/10-%E6%9C%9F%E8%B4%A7%E4%BB%B7%E6%A0%BC%E7%BB%93%E6%9E%84%E6%A3%80%E7%B4%A2%E7%B3%BB%E7%BB%9F/price-structure/scripts/daily_pipeline.py)

下面给你一套最小可落地的嵌入方案：不重构现有系统，只新增一个“图谱上下文编码器”，然后在日更流水线和检索引擎里接入。

---

### **一、嵌入位置总览**

你要改 3 个地方：

| 位置 | 文件 | 作用 |
|---|---|---|
| 新增 | `price-structure/src/graph/embedding.py` | 把知识图谱邻域转成可参与检索打分的特征 |
| 修改 | `price-structure/src/retrieval/engine.py` | 检索时引入 graph bonus，重排近邻 |
| 修改 | `price-structure/scripts/daily_pipeline.py` | 每日编译后把结构增量写入图谱 |

现有 `src/graph/store.py` 已经有 `daily_ingest()`，它会写入结构、Zone、叙事、边，并建立 `evolves_to`、`narrative_evolves`、`transfer_to` 等跨日关系；所以你不需要从零写图数据库，只要把它挂进流水线。 [GitHub](https://raw.githubusercontent.com/doubc/The_Theory_of_Difference/main/10-%E6%9C%9F%E8%B4%A7%E4%BB%B7%E6%A0%BC%E7%BB%93%E6%9E%84%E6%A3%80%E7%B4%A2%E7%B3%BB%E7%BB%9F/price-structure/src/graph/store.py)

---

### **二、新增文件：`src/graph/embedding.py`**

位置：

```text
10-期货价格结构检索系统/
└── price-structure/
    └── src/
        └── graph/
            └── embedding.py    # 新增
```

代码如下：

```python
"""
知识图谱上下文嵌入层

目标：
1. 不替代原有 geometric / relational / motion / family 相似度；
2. 只提供 graph_context_bonus，用于检索结果重排；
3. 使用已有 GraphStore 的 JSONL 数据，不引入新数据库；
4. 让“同 Zone 演化链、同品种结构网络、叙事递归、差异转移”参与检索。
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Any

from src.models import Structure
from src.graph.store import GraphStore


@dataclass
class GraphContext:
    """
    一个结构在知识图谱里的上下文摘要。
    这些字段不是市场预测信号，而是结构关系语义。
    """
    symbol: str
    zone_key: str
    same_zone_count: int = 0
    same_symbol_count: int = 0
    evolution_in_count: int = 0
    evolution_out_count: int = 0
    narrative_count: int = 0
    transfer_out_count: int = 0
    label_count: int = 0

    def to_vector(self) -> list[float]:
        """
        转成一个稳定的小向量。
        用 log 压缩，避免历史越多的品种天然占优。
        """
        import math

        return [
            math.log1p(self.same_zone_count),
            math.log1p(self.same_symbol_count),
            math.log1p(self.evolution_in_count),
            math.log1p(self.evolution_out_count),
            math.log1p(self.narrative_count),
            math.log1p(self.transfer_out_count),
            math.log1p(self.label_count),
        ]


def _safe_symbol(s: Structure) -> str:
    return s.symbol or "unknown"


def _zone_key(s: Structure) -> str:
    symbol = _safe_symbol(s)
    if not s.zone:
        return f"{symbol}:unknown"
    return f"{symbol}:{s.zone.price_center:.0f}"


def _label(s: Structure) -> str:
    return s.label or ""


class GraphEmbedder:
    """
    从 GraphStore 读取知识图谱上下文，并计算结构之间的图谱相似度。
    """

    def __init__(self, store: GraphStore):
        self.store = store
        self._structures: list[dict[str, Any]] | None = None
        self._edges: list[dict[str, Any]] | None = None
        self._narratives: list[dict[str, Any]] | None = None

    @property
    def structures(self) -> list[dict[str, Any]]:
        if self._structures is None:
            self._structures = self.store.load_all_structures()
        return self._structures

    @property
    def edges(self) -> list[dict[str, Any]]:
        if self._edges is None:
            self._edges = self.store.load_all_edges()
        return self._edges

    @property
    def narratives(self) -> list[dict[str, Any]]:
        if self._narratives is None:
            self._narratives = self.store.load_all_narratives()
        return self._narratives

    def context_from_structure(self, s: Structure) -> GraphContext:
        """
        给当前 Structure 构造图谱上下文。
        当前结构可能还没入库，所以用 symbol / zone_key / label 去图谱里找邻域。
        """
        symbol = _safe_symbol(s)
        zone_key = _zone_key(s)
        label = _label(s)

        same_zone = [
            r for r in self.structures
            if r.get("zone_key") == zone_key
        ]

        same_symbol = [
            r for r in self.structures
            if r.get("symbol") == symbol
        ]

        same_label = [
            r for r in self.structures
            if label and r.get("label") == label
        ]

        related_struct_ids = {
            f"struct:{r.get('struct_id')}"
            for r in same_zone
            if r.get("struct_id")
        }

        evolution_in = 0
        evolution_out = 0
        transfer_out = 0

        for e in self.edges:
            et = e.get("edge_type")
            src = e.get("source")
            tgt = e.get("target")

            if et == "evolves_to":
                if tgt in related_struct_ids:
                    evolution_in += 1
                if src in related_struct_ids:
                    evolution_out += 1

            if et == "transfer_to" and src in related_struct_ids:
                transfer_out += 1

        narrative_count = sum(
            1 for n in self.narratives
            if n.get("zone_key") == zone_key
        )

        return GraphContext(
            symbol=symbol,
            zone_key=zone_key,
            same_zone_count=len(same_zone),
            same_symbol_count=len(same_symbol),
            evolution_in_count=evolution_in,
            evolution_out_count=evolution_out,
            narrative_count=narrative_count,
            transfer_out_count=transfer_out,
            label_count=len(same_label),
        )

    def context_from_sample_dict(self, sample_structure: dict, symbol: str = "", label: str = "") -> GraphContext:
        """
        给历史样本构造图谱上下文。
        Sample.structure 是 dict，不一定能完整 rebuild 成 Structure，所以单独处理。
        """
        zd = sample_structure.get("zone", {}) or {}
        sym = symbol or sample_structure.get("symbol") or "unknown"
        center = zd.get("price_center")

        if center is None:
            zone_key = f"{sym}:unknown"
        else:
            zone_key = f"{sym}:{center:.0f}"

        same_zone = [
            r for r in self.structures
            if r.get("zone_key") == zone_key
        ]

        same_symbol = [
            r for r in self.structures
            if r.get("symbol") == sym
        ]

        same_label = [
            r for r in self.structures
            if label and r.get("label") == label
        ]

        related_struct_ids = {
            f"struct:{r.get('struct_id')}"
            for r in same_zone
            if r.get("struct_id")
        }

        evolution_in = 0
        evolution_out = 0
        transfer_out = 0

        for e in self.edges:
            et = e.get("edge_type")
            src = e.get("source")
            tgt = e.get("target")

            if et == "evolves_to":
                if tgt in related_struct_ids:
                    evolution_in += 1
                if src in related_struct_ids:
                    evolution_out += 1

            if et == "transfer_to" and src in related_struct_ids:
                transfer_out += 1

        narrative_count = sum(
            1 for n in self.narratives
            if n.get("zone_key") == zone_key
        )

        return GraphContext(
            symbol=sym,
            zone_key=zone_key,
            same_zone_count=len(same_zone),
            same_symbol_count=len(same_symbol),
            evolution_in_count=evolution_in,
            evolution_out_count=evolution_out,
            narrative_count=narrative_count,
            transfer_out_count=transfer_out,
            label_count=len(same_label),
        )

    @staticmethod
    def cosine(a: list[float], b: list[float]) -> float:
        import math

        if not a or not b or len(a) != len(b):
            return 0.0

        dot = sum(x * y for x, y in zip(a, b))
        na = math.sqrt(sum(x * x for x in a))
        nb = math.sqrt(sum(y * y for y in b))

        if na == 0 or nb == 0:
            return 0.0

        return dot / (na * nb)

    def graph_similarity(self, query: Structure, sample_structure: dict, symbol: str = "", label: str = "") -> float:
        """
        图谱上下文相似度。
        返回 [0, 1]。
        """
        q_ctx = self.context_from_structure(query)
        s_ctx = self.context_from_sample_dict(sample_structure, symbol=symbol, label=label)

        base = self.cosine(q_ctx.to_vector(), s_ctx.to_vector())

        # 强规则：同 Zone 是强关系，但不能直接给满分，避免过拟合。
        same_zone_bonus = 0.15 if q_ctx.zone_key == s_ctx.zone_key else 0.0

        # 同品种是弱关系，因为跨品种结构同态仍然重要。
        same_symbol_bonus = 0.05 if q_ctx.symbol == s_ctx.symbol else 0.0

        return min(1.0, base + same_zone_bonus + same_symbol_bonus)
```

这个文件的作用是把图谱邻域变成一个很轻的向量：同 Zone 历史结构数、同品种结构数、演化入边、演化出边、叙事数、差异转移边、同标签结构数。它不是“预测模型”，只是让相似检索知道：两个结构不只是形态像，它们在历史结构网络里的位置也像。

---

### **三、修改 `src/retrieval/engine.py`：让图谱参与检索重排**

位置：

```text
price-structure/src/retrieval/engine.py
```

#### **1. 修改 import 区**

在原有 import 后面加：

```python
from src.graph.store import GraphStore
from src.graph.embedding import GraphEmbedder
```

#### **2. 修改 `RetrievalEngine.__init__`**

把原来的：

```python
class RetrievalEngine:
    """相似结构检索引擎（V1.6 P1 升级）"""
    def __init__(self, store: SampleStore):
        self.store = store
```

改成：

```python
class RetrievalEngine:
    """相似结构检索引擎（加入知识图谱上下文重排）"""

    def __init__(
        self,
        store: SampleStore,
        graph_store: GraphStore | None = None,
        graph_weight: float = 0.12,
    ):
        self.store = store
        self.graph_store = graph_store
        self.graph_weight = graph_weight
        self.graph_embedder = GraphEmbedder(graph_store) if graph_store else None
```

#### **3. 修改 `Neighbor` 数据结构**

把原来的：

```python
@dataclass
class Neighbor:
    """一个近邻样本 + 相似度 + 匹配归因"""
    sample: Sample
    score: SimilarityScore
    match_reason: str = ""
```

改成：

```python
@dataclass
class Neighbor:
    """一个近邻样本 + 相似度 + 匹配归因"""
    sample: Sample
    score: SimilarityScore
    match_reason: str = ""
    graph_score: float = 0.0
    final_score: float = 0.0
```

#### **4. 在 `retrieve()` 中替换加入候选的逻辑**

你在 `retrieve()` 里面有两处类似逻辑。

第一处在 numpy/C 加速分支里：

```python
if sc.total >= min_score:
    reason = _generate_match_reason(query, s2, sc)
    scored.append(Neighbor(sample=sp, score=sc, match_reason=reason))
```

替换为：

```python
if sc.total >= min_score:
    reason = _generate_match_reason(query, s2, sc)

    graph_score = 0.0
    if self.graph_embedder:
        graph_score = self.graph_embedder.graph_similarity(
            query=query,
            sample_structure=sp.structure,
            symbol=sp.symbol,
            label=sp.label_type,
        )

    final_score = (1.0 - self.graph_weight) * sc.total + self.graph_weight * graph_score

    if graph_score > 0.65:
        reason += f"，图谱上下文相似({graph_score:.2f})"

    scored.append(
        Neighbor(
            sample=sp,
            score=sc,
            match_reason=reason,
            graph_score=graph_score,
            final_score=final_score,
        )
    )
```

第二处在 fallback 分支里：

```python
if sc.total >= min_score:
    reason = _generate_match_reason(query, s2, sc)
    scored.append(Neighbor(sample=sp, score=sc, match_reason=reason))
```

也替换成同样的代码。

#### **5. 修改排序逻辑**

把原来的：

```python
scored.sort(key=lambda n: n.score.total, reverse=True)
```

改成：

```python
scored.sort(
    key=lambda n: n.final_score if n.final_score > 0 else n.score.total,
    reverse=True,
)
```

这样改完之后，原有相似度仍然是主体，知识图谱只是做小权重重排。推荐 `graph_weight=0.08 ~ 0.15`，不要太高，否则会把“历史关系丰富的品种”误判为“更相似”。

---

### **四、修改 `scripts/daily_pipeline.py`：日更时写入知识图谱**

位置：

```text
price-structure/scripts/daily_pipeline.py
```

#### **1. 在 import 区加入**

```python
from src.graph.store import GraphStore
```

#### **2. 在编译完成之后、规则扫描之前嵌入图谱写入**

找到这段：

```python
result = compile_full(bars, config)
print(f" {len(result.structures)} 结构, {len(result.bundles)} 丛")

# 3. 规则扫描
print(" [3] 规则扫描...")
```

中间插入：

```python
# 2.5 知识图谱增量写入
print(" [2.5] 知识图谱增量写入...")
graph_store = GraphStore("data/graph")
graph_stats = graph_store.daily_ingest(result.structures, symbol="CU000")
print(
    f" 图谱写入: structures={graph_stats.get('structures_ingested', 0)}, "
    f"zones={graph_stats.get('zones_ingested', 0)}, "
    f"narratives={graph_stats.get('narratives_ingested', 0)}, "
    f"edges={graph_stats.get('edges_ingested', 0)}, "
    f"evolution={graph_stats.get('evolution_edges', 0)}, "
    f"snapshot={graph_stats.get('snapshot', '')}"
)
```

#### **3. 在检索引擎初始化处接入 `graph_store`**

找到：

```python
store = SampleStore("data/samples/library.jsonl")
engine = RetrievalEngine(store)
```

改成：

```python
store = SampleStore("data/samples/library.jsonl")
engine = RetrievalEngine(
    store,
    graph_store=graph_store,
    graph_weight=0.12,
)
```

这样日更流程就变成：

```text
加载数据
→ 编译结构
→ 写入知识图谱
→ 规则扫描
→ 质量分层
→ 生命周期记录
→ 相似检索，检索时使用图谱上下文重排
→ 输出报告
```

---

### **五、可选：让日报显示图谱分数**

位置仍然是：

```text
price-structure/scripts/daily_pipeline.py
```

找到日报生成里这段：

```python
if ret.neighbors:
    p = ret.posterior
    report_lines.append(f"**后验 (n={p.sample_size})**:")
```

你可以在它前面加一个“近邻解释”：

```python
if ret.neighbors:
    report_lines.append("**近邻匹配解释**:")
    for nb in ret.neighbors[:3]:
        report_lines.append(
            f"- {nb.sample.id}: "
            f"sim={nb.score.total:.2f}, "
            f"graph={nb.graph_score:.2f}, "
            f"final={nb.final_score:.2f}, "
            f"{nb.match_reason}"
        )
    report_lines.append("")
```

这样你每天的 Markdown 报告里会看到：

```text
近邻匹配解释:
- CU_0031: sim=0.74, graph=0.69, final=0.73, 几何形态高度一致，图谱上下文相似(0.69)
```

这一步很重要，因为知识图谱不能黑箱加分，必须能解释它为什么影响排序。

---

### **六、最终目录结构应该变成这样**

```text
price-structure/
├── data/
│   ├── graph/
│   │   ├── structures.jsonl
│   │   ├── zones.jsonl
│   │   ├── narratives.jsonl
│   │   ├── edges.jsonl
│   │   ├── index/
│   │   │   ├── by_zone.json
│   │   │   ├── by_symbol.json
│   │   │   └── by_narrative.json
│   │   └── snapshots/
│   │       └── 2026-04-25.json
│   └── samples/
│       └── library.jsonl
├── scripts/
│   └── daily_pipeline.py          # 修改：写入图谱 + 检索接入 graph_store
└── src/
    ├── graph/
    │   ├── __init__.py            # 已有：StructureGraph
    │   ├── store.py               # 已有：GraphStore
    │   └── embedding.py           # 新增：图谱上下文嵌入
    └── retrieval/
        └── engine.py              # 修改：graph_score / final_score 重排
```

---

### **七、为什么这样嵌入，而不是直接改 `similarity.py`？**

`src/retrieval/similarity.py` 现在负责的是结构内部相似度：几何、关系、运动、结构族，后面还扩展了 DTW 和段级形状相似。它处理的是“两个结构对象本身像不像”。知识图谱处理的是“两个结构在历史关系网络中的位置像不像”。这两个层次不同，强行塞进 `similarity.py` 会让底层相似度函数依赖文件系统和图谱状态，后面测试会变脏。现在的做法是：`similarity.py` 保持纯函数，`engine.py` 做检索编排和重排，这样结构更干净。 [GitHub](https://raw.githubusercontent.com/doubc/The_Theory_of_Difference/main/10-%E6%9C%9F%E8%B4%A7%E4%BB%B7%E6%A0%BC%E7%BB%93%E6%9E%84%E6%A3%80%E7%B4%A2%E7%B3%BB%E7%BB%9F/price-structure/src/retrieval/similarity.py) [GitHub](https://raw.githubusercontent.com/doubc/The_Theory_of_Difference/main/10-%E6%9C%9F%E8%B4%A7%E4%BB%B7%E6%A0%BC%E7%BB%93%E6%9E%84%E6%A3%80%E7%B4%A2%E7%B3%BB%E7%BB%9F/price-structure/src/retrieval/engine.py)

---

### **八、运行方式**

在 `price-structure` 目录下运行：

```bash
python3 scripts/daily_pipeline.py
```

第一次运行后会自动生成：

```text
data/graph/
├── structures.jsonl
├── zones.jsonl
├── narratives.jsonl
├── edges.jsonl
├── index/
└── snapshots/
```

后续每天运行，`GraphStore.daily_ingest()` 会继续追加节点和边，并保存当日快照。检索时 `RetrievalEngine` 会用图谱上下文对相似样本做轻量重排。

这个方案的核心原则是：**结构相似度负责“形态像不像”，知识图谱负责“历史关系位置像不像”，两者加权融合，但不互相污染。**